action (GetInfo) {
  type (Search)
  description (의약품 낱알식별정보조회)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }
  }
  output (DetailInfo) // 필요하면 DetailInfo로 변경해도 됩니다
}